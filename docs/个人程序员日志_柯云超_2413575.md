# 个人程序员日志（第 1 天）

项目名称：基于 Claude Skill 的竞品动态追踪与智能对标分析系统实战

小组名称：智竞先锋  
姓名：柯云超  
学号：2413575  
角色：PG  
日期：2026年7月6日

## 一、今日完成工作

1. 参与梳理项目业务功能说明，明确系统需要覆盖竞品数据采集、数据清洗、结构化解析、多 Agent 分析、风险预警、报告生成和可视化接口等核心能力。

2. 参与技术架构分析，明确 Claude + LangChain + Multi-Agent + RAG + FastAPI 的五层架构，包括大模型基础层、数据处理与知识检索层、智能体与工具层、服务化接口层、展示与交付层。

3. 根据小组讨论结果整理核心模块，包括竞品配置模块、公开舆情采集模块、价格监控 Agent、新品检测 Agent、负面舆情 Agent、风险预警模块、报告生成模块、RAG 知识库模块和可视化接口模块。

4. 参与制定软件开发模式，采用迭代式开发、小组敏捷协作和分阶段交付方式推进，按照 20 天实训节奏完成立项、开发、测试和结项。

5. 参与明确工程规范，包括项目目录结构、安全配置规范、Prompt 模板管理规范、数据溯源规范、代码规范和文档提交规范。

6. 完成个人开发环境检查，确认 Python 虚拟环境、依赖配置、`.env` 配置和 Claude API 连通性测试可用。测试命令为 `uv run python verify_env.py`，结果显示 Anthropic 兼容接口与 LangChain pipe 调用成功，模型为 `deepseek-v4-flash`。

7. 协助准备项目初始骨架，新增 `main.py`、`modules/`、`data/raw/`、`data/processed/`、`data/reports/`、`chroma_db/` 等目录与基础文件，并补充 README 和需求规格说明书相关材料。

## 二、开发过程遇到的问题与解决方案

1. 问题：项目初始 README 只描述 Anthropic + LangChain 环境，没有体现竞品情报系统的业务目标、目录结构和运行方式。

   解决：重新整理 README，补充项目简介、目录结构、环境配置、API 连通性测试结果、服务启动方式和后续计划。

2. 问题：项目目录中缺少 `modules/`、`data/`、`chroma_db/` 等实际开发骨架，后续编码入口不清晰。

   解决：创建基础目录和模块文件，将 Agent、Prompt、RAG、工具函数和 FastAPI 入口分别放入独立文件，保证后续开发可以按模块推进。

3. 问题：竞品分析结论容易出现无来源支撑的模型生成内容。

   解决：在工程规范和文档中明确要求所有情报结论必须绑定公开数据源，原始舆情数据需保存来源链接、采集时间和原始文本。

## 三、未完成任务与延期原因

1. 未完成内容：尚未接入真实公开舆情数据源，当前仅完成数据目录和 RAG 模块占位结构。

   延误原因：今天重点是完成项目说明、技术设计、工程规范和初始环境搭建，真实数据采集安排在后续开发阶段。

2. 未完成内容：价格监控 Agent、新品检测 Agent、负面舆情 Agent 目前是基础占位实现，尚未接入 Claude 模型和 RAG 检索结果。

   延误原因：需要先补充测试数据、Prompt 模板和结构化输出 Schema，再进行 Agent 联调。

## 四、明日工作计划

1. 准备公开舆情测试数据，至少覆盖价格战、新品发布、负面舆情三类样例。
2. 完成数据清洗工具，支持去重、正文抽取、时间标准化和来源留存。
3. 设计情报卡片 JSON Schema，明确 Agent 输出字段。
4. 完善 Prompt 模板，分别编写价格、新品、舆情和简报生成 Prompt。
5. 初步接入 RAG 检索流程，为后续多 Agent 分析提供证据片段。

## 五、项目规范执行自查（对照竞品情报工程规范）

1. 代码目录是否规范：☑是 □否，问题：已创建 `main.py`、`modules/`、`data/`、`chroma_db/`、`docs/`。
2. Prompt 是否统一存放 prompts.py、无硬编码：☑是 □否，问题：已创建 `modules/prompts.py`，后续模板统一放入该文件。
3. 敏感密钥是否写入 .env、无硬编码：☑是 □否，问题：API Key 通过 `.env` 读取，文档未记录明文密钥。
4. 数据是否留存数据源，分析结论可溯源：☑是 □否，问题：规范已明确，真实数据接入后需要严格执行。
5. 代码是否添加基础注释、异常捕获：☑是 □否，问题：基础模块已具备清晰职责，后续业务代码继续补充异常处理。

## 六、其他备注

今天不是直接完成完整系统，而是完成项目说明、技术设计、工程规范、开发环境和项目骨架，为后续编码和联调打基础。个人工作应与小组材料区分：小组材料记录项目整体情况，个人日志记录本人参与和完成的具体事项。
